# Dipendenze dell'app finanza personale (tutte gratuite, installate in locale).
# Il necessario per far girare l'app in locale; i dati di mercato
# arrivano via HTTP (Yahoo Finance), senza librerie aggiuntive.
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
sqlalchemy>=2.0
python-multipart>=0.0.9

# Opzionali: servono SOLO se scegli il provider "Vertex AI" per l'agente
# (Impostazioni → Agente AI → Provider). Con il default "Google AI Studio" non
# sono necessarie. google-auth firma il token del service account; requests è il
# suo trasporto per il refresh. Guida: docs/SETUP-VERTEX.md.
google-auth>=2.23
requests>=2.31
